service:
  port: 80

ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    kubernetes.io/tls-acme: 'true'
  hosts:
    - host: docs.octusbridge.io
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: docs.octusbridge.io-tls
      hosts:
        - docs.octusbridge.io

livenessProbe:
  tcpSocket:
    port: 80
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  tcpSocket:
    port: 80
  initialDelaySeconds: 5
  periodSeconds: 10
