import { ProviderRpcClient } from "everscale-inpage-provider";
import { AllowedCodes, TraceParams } from "locklift/internal/tracing/types";
import { Factory } from "locklift/internal/factory";
import { TracingInternal } from "locklift/internal/tracing/tracingInternal";
import { Transactions } from "locklift/utils";
import { TransactionParameter } from "locklift/types";
import { ViewTracingTree } from "locklift/internal/tracing/viewTraceTree/viewTracingTree";
export declare class Tracing {
    private readonly ever;
    private readonly tracingInternal;
    private readonly features;
    constructor(ever: ProviderRpcClient, tracingInternal: TracingInternal, features: Transactions);
    setContractLabels: TracingInternal["setContractLabels"];
    trace: <T extends TransactionParameter>(transactionProm: T | Promise<T>, config?: Omit<TraceParams, "inMsgId">) => Promise<T & {
        traceTree: ViewTracingTree | undefined;
    }>;
    get allowedCodes(): AllowedCodes;
    setAllowedCodes: (allowedCodes: import("locklift/internal/tracing/types").OptionalContracts) => void;
    setAllowedCodesForAddress: (address: string | import("everscale-inpage-provider").Address, allowedCodes: import("locklift/internal/tracing/types").OptionalContracts) => void;
    removeAllowedCodesForAddress: (address: string | import("everscale-inpage-provider").Address, codesToRemove: import("locklift/internal/tracing/types").OptionalContracts) => void;
    removeAllowedCodes: (codesToRemove: import("locklift/internal/tracing/types").OptionalContracts) => void;
}
export declare const createTracing: ({ ever, factory, features, endpoint, }: {
    ever: ProviderRpcClient;
    factory: Factory<any>;
    features: Transactions;
    endpoint?: string | undefined;
}) => Tracing;
