import { ProviderRpcClient } from "everscale-inpage-provider";
import { SimpleKeystore } from "everscale-standalone-client/nodejs";
import { ConfigState, LockliftConfig } from "locklift/internal/config";
import * as utils from "locklift/utils";
import { Transactions } from "locklift/utils";
import { Factory, FactoryType, Giver } from "locklift/internal/factory";
import { Tracing } from "locklift/internal/tracing";
import { TimeMovement } from "locklift/internal/timeMovement";
import { LockliftContext } from "locklift/internal/context/lockliftContext";
import "locklift/chaiPlugin/types";
export * from "everscale-inpage-provider";
export type { Signer } from "everscale-standalone-client";
export { Dimension, zeroAddress } from "locklift/constants";
export type { LockliftConfig } from "locklift/internal/config";
export type { Giver } from "locklift/internal/factory";
export { toNano, fromNano, getRandomNonce, convertAmount } from "locklift/utils";
export { WalletTypes } from "locklift/types";
export { TraceType, InteractionType } from "locklift/internal/tracing/types";
export { lockliftChai } from "locklift/chaiPlugin";
export { NetworkValue, ConfigState } from "locklift/internal/config";
export declare class Locklift<FactorySource extends FactoryType> {
    #private;
    readonly provider: ProviderRpcClient;
    readonly keystore: SimpleKeystore;
    private readonly clock;
    readonly transactions: Transactions;
    readonly utils: typeof utils;
    private constructor();
    set tracing(tracing: Tracing);
    get tracing(): Tracing;
    set testing(testing: TimeMovement);
    get testing(): TimeMovement;
    set context(context: LockliftContext);
    get context(): LockliftContext;
    set factory(factory: Factory<FactorySource>);
    get factory(): Factory<FactorySource>;
    set giver(giver: Giver);
    get giver(): Giver;
    static setup<T extends FactoryType>(config: LockliftConfig<ConfigState.INTERNAL>, network?: keyof LockliftConfig["networks"]): Promise<Locklift<T>>;
}
